# Généré au premier lancement (voir README.md) — jamais à committer.
python-embed/
ffmpeg/
data/
logs/

# Contient potentiellement des mots de passe/tokens réels (auth, admin,
# guest_api, media_api, upload_token) une fois personnalisé pour un
# événement — jamais à committer. Voir config/config.example.toml pour un
# gabarit sans secrets, à copier en config.toml lors de l'installation.
config/config.toml


# Python
__pycache__/
*.pyc
.venv/

# Éditeurs / OS
.vscode/
.idea/
Thumbs.db
.DS_Store
